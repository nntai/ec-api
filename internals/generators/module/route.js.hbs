const BaseRouter = require('../base/base.route');
const {{camelCase name}}Controller = require('./{{camelCase name}}.controller');

class {{properCase name}}Router extends BaseRouter {
  constructor() {
    super({{camelCase name}}Controller);
  }
  setup() {
    const router = this.init();

    // TODO: setup routers here

    return router;
  }
}

module.exports = new {{properCase name}}Router();
