import mongoose from 'mongoose';
const { Schema } = mongoose;

const {{camelCase name}}Schema = new Schema({
  // schema here
});

export default mongoose.model('{{properCase name}}', {{camelCase name}}Schema);
